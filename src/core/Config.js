/**
 * Stage 8 — Virtual Gallery configuration.
 *
 * Set POCKETBASE_URL to your running PocketBase instance before using
 * the "Share to Gallery" feature.  The collection name must match what
 * you created in the PocketBase admin UI (default: pc_gallery).
 */
export const POCKETBASE_URL      = 'https://habit-tracker.pockethost.io/';
export const GALLERY_COLLECTION  = 'pc_gallery';
export const RATINGS_COLLECTION  = 'pc_ratings';
